import { Box, List } from "@chakra-ui/layout";
import React from "react";

interface ModListProps {
  recipeId: number;
}

export const ModList: React.FC<ModListProps> = ({recipeId}) => {
  // modquery list

  return <Box>
    <List>
      {/* Modlist */}
    </List>
  </Box>;
};
